# vLLM 0.26.0 NVFP4 TP=2 profile at 200K

Date: 2026-07-27

## Config

- Model: `nvidia/Qwen3.6-27B-NVFP4`
- Image: `vllm-xpu-env:int8g-v0260`
- Runtime: vLLM 0.26.0, torch 2.12.0+xpu,
  vllm-xpu-kernels 0.1.11.1
- TP=2, `MAXLEN=200000`, `UTIL=0.85`, `MAXSEQS=8`
- PIECEWISE graph, MTP5, calibrated fp8 KV, native E4M3 NVFP4
  scales for M<=8
- Prefix cache enabled
- Graph push-all-reduce enabled, `PUSH_AR_MAXB=268435456`
- `CCL_TOPO_P2P_ACCESS=0`

The profile followed the full long-context qualification, which passed an
exact 190,048-token retrieval and a 52K-token concurrent soak.

## Command

Each mode used a fresh server because repeated Kineto start/stop has been
unreliable in one XPU worker:

```bash
./bin/gpu-run bash vllm/nvfp4/profile_tp2_v0260.sh decode
./bin/gpu-run bash vllm/nvfp4/profile_tp2_v0260.sh prefill
```

The decode request generated 256 forced tokens. The prefill request used
5,210 prompt tokens and one output token. Runtime traces are under:

```text
/mnt/vm_8tb/b70/profiles/v0260_tp2_decode_vVFh6N/
/mnt/vm_8tb/b70/profiles/v0260_tp2_prefill_HN77td/
```

## Result

### Decode

Rank 1 is the representative trace:

| category | device time | share |
|---|---:|---:|
| all-reduce / collective | 491.26 ms | 41.2% |
| linear GEMM | 429.07 ms | 36.0% |
| GDN / mamba scan | 115.18 ms | 9.7% |
| attention | 75.26 ms | 6.3% |
| elementwise / copy | 49.00 ms | 4.1% |
| sampling / logits | 26.33 ms | 2.2% |

The largest individual kernel is 625
`oneccl_allreduce_pcie<bf16>` calls: 464.16 ms, or 39.0% of total
device time. These are the eager full-vocabulary MTP gathers. The graph
push kernel `do_ar` accounts for 25.98 ms, or 2.2%.

Rank 0 recorded the same kernel counts but reported the 625 oneCCL
kernels as 5.76 seconds instead of 0.46 seconds. This is profiler-induced
rank asymmetry, not a serving result: rank 1 matches the earlier v0.25.1
trace and the unprofiled server has balanced TP behavior. Do not average
the two ranks.

### Fused local top-1 decode

The default-off fused local-argmax path was profiled separately with
`LOCALARGMAX=1 LOCALARGMAX_FUSED_FIX=1` and the prototype extension.
The rank-1 trace is representative:

| category | device time | share |
|---|---:|---:|
| linear GEMM | 699.74 ms | 39.1% |
| all-reduce / collective | 490.34 ms | 27.4% |
| sampling / logits | 188.41 ms | 10.5% |
| GDN / mamba scan | 186.25 ms | 10.4% |
| attention | 124.81 ms | 7.0% |
| elementwise / copy | 90.49 ms | 5.1% |

The collective message shapes identify what changed:

| message | baseline count | fused count |
|---|---:|---:|
| bf16 248,320 elements, draft M=1 full vocabulary | 521 | 1 |
| bf16 1,489,920 elements, target M=6 verification | 103 | 172 |
| fp32 4 elements, two shard value/index pairs | 0 | 865 |
| bf16 46,080 elements, small setup/other gather | 1 | 1 |

The replacement worked: 520 per-draft full-vocabulary messages disappeared.
On rank 1, the 865 compact pair reductions consumed only 9.72 ms of device
kernel time, or 0.0112 ms each. The fused shard top-1 kernel consumed
163.03 ms, or 0.188 ms each under profiling. The remaining 430.94 ms of
large oneCCL time was the 172 target-verification M=6 gathers at 2.506 ms
each, essentially the baseline's 2.492 ms per M=6 gather. Target
verification still needs the full distributions for speculative rejection
and cannot use an argmax-only reduction.

The compact path is nevertheless host-latency-bound. Its enclosing synchronous
`vllm::all_gather` scopes averaged 5.27 ms on rank 0 and 15.93 ms on rank 1
under profiling, compared with 3.10/6.55 ms for the baseline draft gather.
Those host timings are profiler-inflated and rank-asymmetric, so they are not
unprofiled wall-clock measurements. They do explain the measured shape of the
result: c1 regressed from 48.9 to 32.5 t/s while c4 improved from 103.0 to
115.7 aggregate because the fixed per-draft synchronization is amortized by
concurrency.

The two captures traversed different numbers of target steps: 103 baseline
M=6 gathers versus 172 fused. Consequently, raw total GEMM, GDN, attention,
and target-gather times are not a direct A/B. The old trace driver recorded
neither a request seed nor an output hash, so that difference cannot be
resolved retrospectively. The driver now fixes seed 1701 and prints the
response SHA256 for future paired captures.

Rank 0 again suffered profiler skew: its 865 compact oneCCL kernels were
reported as 22.99 seconds versus 9.72 ms on rank 1. Reject rank 0 timing;
the message counts still agree exactly.

### Prefill

The ranks agree closely:

| category | rank 0 | rank 1 |
|---|---:|---:|
| linear GEMM | 53.1% | 52.3% |
| all-reduce / collective | 32.8% | 33.7% |
| GDN / mamba scan | 5.4% | 5.4% |
| attention | 5.1% | 5.0% |
| elementwise / copy | 1.7% | 1.6% |

On rank 1, graph push kernels consume about 673.5 ms (31.4% of total)
while oneCCL consumes only 49.3 ms (2.3%). The large prefill
collectives therefore reach the intended push transport.

## Verdict

The v0.26.0 profile reconfirms the optimization ordering:

1. Decode is communication-bound, but the 2026-07-28 validation found a
   concurrency tradeoff rather than a universal replacement. The custom
   fused shard top-1 op was token-exact and raised c4 aggregate from
   103.0 to 115.7 t/s, but cut c1 from 48.9 to 32.5. Falling back to the
   full gather at M=1 restored c1 to 48.6 but regressed c4 to 93.4 as
   active batch shapes switched.
2. Do not spend the next session replacing the NVFP4 GEMM. The existing
   decode GEMM is already at the measured B70 memory-bandwidth roofline.
3. Prefill push-all-reduce is working. Its 33-34% share is real work on
   the fast transport, not an accidental oneCCL fallback.
4. The fused profile proves the next local-argmax target is the number of
   synchronous compact collectives, not their 0.011 ms device kernel.
   Lower depth did not solve it: fused MTP3 raised c1 to 37.2 but reduced
   c4 to the 103.0 shelf baseline, while fused MTP4 emitted visible `!`
   garbage in 1/18 streams. The static-depth branch is closed. The large
   target-verify gather remains, while GDN is a secondary 9.7-10.4% target.

The env-gated local-argmax implementations remain default-off. The fused
form is useful evidence and a possible explicit throughput mode, but it
does not meet the shelf requirement of a coherent best static setting
without sacrificing interactive speed.
