# Qwen3.8-27B W4A8 full-send -- loop ledger

Standing prompt: `docs/20260820_qwen38_w4a8_campaign.md`.
Campaign journal: `docs/20260820_qwen38_w4a8_journal.md`.
Dead-ends: `docs/20260820_qwen38_w4a8_deadends.md`.

Newest loop at the bottom. Next agent reads the last 3 + NEXT PICK.

---

## NEXT PICK

GPTQ fire 2 is RUNNING on card 0. GRAPH=0 serve Up `:18081`.

- Card 0: ATTACH `w4a8-151-gptq` / log `results/logs/151_qwen38_w4a8_20260821_010557.log`. Do not start a second 151.
- Card 1: do not start a second serve on `qwen38_w4a8_rtn`. GRAPH=1 A/B only after `bash vllm/w4a8/serve_qwen38_w4a8.sh stop`.
- When GPTQ Stage B GO: wrap_vlm_config.py (includes in_proj_qkvz) then census.
- Do not bake. Do not start DD. P2PACCESS=0.

30m loop: ARMED `01a021be5649`.

---

## LOOP 0 -- 2026-08-20 -- plan + 151 producer (no GPU)

Picked: unblock (write the successor-session campaign)
Why this, not the other open row: operator asked for a full-send W4A8 plan a new Grok session can start, with card0 quant / card1 kernels in parallel
GPU: none. `gpu-run --status` both free at write
Command: docs + copy 149 -> 151
Log: n/a
Result: `docs/20260820_qwen38_w4a8_campaign.md`; `scripts/151_quantize_qwen38_27b_w4a8.sh` (3.8 paths, int8g-v0260, default DATAFREE=1 -> `models/files/qwen3.8-27b/w4a8-rtn-gdn`)
Verdict: GO for the new session to fire day-1
Changed beliefs: A-E serial in `docs/20260820_b70_bw_campaigns.md` is parked; this window jumps F and absorbs A/B/D/E as K-loops. No public 3.8 W4A8 HF card.
Next pick: dual-card day-1 (see top)
Do not: bake three images before 151 Stage A is running
Restore: DD stays down
JOURNAL: ### 2026-08-20bv

---

## LOOP 1 -- 2026-08-21T0017Z -- card0 151 DATAFREE=1 RTN W4A8+GDN

Picked: producer fire 1 (campaign 8.1)
Why this, not the other open row: dual-card day-1; no 3.8 W4A8 file exists
GPU: card0 locked `w4a8-151-rtn`. card1 K1 in parallel. DD down. P2PACCESS unset.
Command:
  B70_GPU_LOCK_TIMEOUT=0 B70_AGENT=w4a8-151-rtn ./bin/gpu-run --card 0 \
    env DATAFREE=1 CARD=0 bash scripts/151_quantize_qwen38_27b_w4a8.sh
Log: results/logs/151_qwen38_w4a8_20260821_001757.log
Result: 443s, exit 0. DataFreePipeline 119s. Stage B pack 256 int4 + 144 I8 GDN.
  OUT models/files/qwen3.8-27b/w4a8-rtn-gdn 20.616 GiB (model 18.967 + vis 0.858 + mtp 0.791).
  GDN named_modules hit 144/144. Root-level "Could not match re:.*linear_attn..." is a
  red herring (regex vs Qwen3_5ForCausalLM instance); nested Linears quantized.
  Saved config group_0=INT8 GDN, group_1=INT4 Linear (swapped numbers vs 151 source; bits/strategy correct).
  Arch saved as Qwen3_5ForCausalLM / qwen3_5_text (AutoModelForCausalLM path). Vision+MTP grafted 333/15.
  compressed-tensors 0.18.0 in the live container (vllm image wants 0.17.0).
  Host `rm -rf RAW` bounced (root-owned); removed later via docker. chown 1000 after.
Verdict: GO
Changed beliefs: DATAFREE RTN on 3.8 is minutes not hours (CPU DataFreePipeline). Two-group GDN INT8 recipe holds.
  151 does not chown OUT; next fire must chmod or serve-as-root.
Next pick: K0 census then GRAPH=0 smoke. GPTQ fire 2 after load-gate.
Do not: bake images; start DD; P2PACCESS=1; rewrite 151; run offline_prepack_w4a8.py on this dir (D12)
Restore: card0 released. DD stays down. RAW removed.
JOURNAL: ### 2026-08-21a

---

## LOOP 2 -- 2026-08-21T0018Z -- card1 K1 isolated kernel matrix

Picked: K1
Why this, not the other open row: day-1 card1 while 151 ran; 3.6 probe is down_proj-only
GPU: card1. Image int8g-v0260 + fusedq .so. ZE_AFFINITY_MASK=1. P2PACCESS unset.
Command:
  B70_GPU_LOCK_TIMEOUT=0 B70_AGENT=w4a8-k1 ./bin/gpu-run --card 1 \
    bash vllm/w4a8/run_k1_matrix.sh
  Harness: vllm/w4a8/bench_w4a8_shapes.py (copy/extend of sglang/int4_gemm_w4a8_probe.py)
Log: results/logs/k1_w4a8_shapes_20260821T001857Z.log
CSV: results/logs/k1_w4a8_shapes_20260821T001857Z.csv  n=441
Result: first docker died (int8g-v0260 Entrypoint=`sleep`; sleep -c). Rerun with --entrypoint bash, 25s, exit 0.
  Ops present: int4_gemm_w4a{8,16}, int8_gemm_w8a{8,16}, fusedq, dynamic_per_token_int8_quant.
  K1 GATE (3.6-sqgptq stand-in):
    gate_up M=1  w4a16 0.161 ms 552 GB/s 95%roof 3.72x bf16; w4a8_op 0.159 / 96%; w4a8_full 0.172 / 89%
    gate_up M=8  w4a16 0.164 / 93%; w4a8_op 0.164 / 94%
    down_proj M=1 w4a16 0.079 ms 565 GB/s 97%roof 3.91x; w4a8_op 0.080 / 96%; w4a8_full 0.097 / 79%
    down_proj M=8 w4a16 0.080 / 96%; w4a8_full 0.098 / 78%
    gate_up M=2048 w4a8_op 3.71 ms 197 TOPS; w8a8_full 2.81 ms 260 TOPS; w4a16 5.41 ms (loses)
  gdn_ba N=96: all paths <3% of 581. w4a8_full 0.65x bf16 at M=1. Trap holds; keep BF16.
  Path S proto_int4 card1: s8 208.2 TOPS, s4 557.9, s2 559.6 (tile microbench, not a serve).
Verdict: GO. Split-M: decode stays Path H (w4a16); M>=256 / prefill Path X w4a8_op beats H; native W8A8 wins large-M TOPS.
Changed beliefs: isolated SYCL act-quant tax on down_proj M=1 is ~17 us, not 101 us (101 us was eager Python).
  int8g-v0260 Entrypoint is leftover sleep -- always --entrypoint bash.
  N=96 is not an XMX citizen. Pad-M=1-to-8 is still a later whacky (M=8 is still BW-bound, ~17 TOPS).
Next pick: K0 census (card0 artifact) then GRAPH=0 smoke
Do not: mix H and X without naming the M=1 op; torch.compile act-quant (D05); joint_matrix as the path (D04 still one re-probe later)
Restore: card1 released after 25s. DD stays down.
JOURNAL: ### 2026-08-21b

---

## LOOP 3 -- 2026-08-21T0026Z -- K0 file census of w4a8-rtn-gdn

Picked: K0 dtype/bytes table (dispatch proof deferred to GRAPH=0 smoke)
Why this, not the other open row: campaign fail-closed before any speed claim on the 151 file
GPU: none (CPU safetensors). Both cards free after LOOP 1/2.
Command: python census of models/files/qwen3.8-27b/w4a8-rtn-gdn
Log: results/logs/k0_census_w4a8_rtn_gdn_20260821.txt
Result: 20.616 GiB / 1599 tensors. MLP I32 7.969 + attn I32 0.781 = 8.75 GiB packed int4.
  GDN fat I8 5.156 GiB (144, [N,1] scale) -- not BF16. gdn_other BF16 0.048 (ba/conv/A_log).
  lm_head+embed BF16 2.368 each. vis 0.858 mtp 0.791. hot no vis/mtp 18.967 GiB.
  Samples: down_proj [5120,2176] I32; q_proj [12288,640] I32; in_proj_qkv [10240,5120] I8; in_proj_b [48,5120] BF16.
  is_prepacked_w4a8=True. No blanket linear_attn ignore.
Verdict: GO on file census. Dispatch (`int4_gemm_w4a8` called in serve) still open.
Changed beliefs: expected ~20.6 GiB was right. 3.8 W4A8+GDN-INT8 artifact exists. Config is text CausalLM.
Next pick: GRAPH=0 vLLM smoke card 1, served id qwen3.8-27b-W4A8-rtn-gdn
Do not: quote DRAM-roof tok/s as fact without a serve; GPTQ fire 2 before load-gate; bake
Restore: DD stays down. Artifact readable uid 1000.
JOURNAL: ### 2026-08-21c

---

## LOOP 4 -- 2026-08-21T0035Z -- campaign journal + arm 30m loop

Picked: unblock (operator: own journal + /loop 30m if not armed)
Why this, not the other open row: scheduler_list was empty; GRAPH=0 smoke is the GPU pick for the first fire
GPU: none this note. Cards free.
Command: wrote docs/20260820_qwen38_w4a8_journal.md; pointed campaign section 3/13 here; scheduler_create 30m durable fire_immediately
Log: n/a
Result: campaign journal is the evidence log. Root JOURNAL.md is a one-line pointer. Scheduler 01a021be5649 armed.
Verdict: GO (steer). Next loop starts on arm, then every 30m. First GPU pick remains GRAPH=0 smoke.
Changed beliefs: non-durable schedulers die across grok restart (2026-08-19a). This one is durable.
Next pick: GRAPH=0 vLLM smoke card 1 (the first 30m fire)
Do not: start DD; bake; GPTQ before load-gate; P2PACCESS=1
Restore: DD stays down
JOURNAL: ### 2026-08-21d (campaign journal)

---

## LOOP 5 -- 2026-08-21T0041Z -- GRAPH=0 vLLM smoke load-gate GO

Picked: K0 dispatch + K3 GRAPH=0 e2e smoke
Why this, not the other open row: NEXT PICK; load-gate before GPTQ
GPU: card1. NAME=qwen38_w4a8_rtn PORT=18081. DD down. P2PACCESS=0
Command: wrap_vlm_config.py; serve_qwen38_w4a8.sh start GRAPH=0 NOMM=1 B70_NOMTP=1
Log: results/logs/l5b_w4a8_rtn_graph0_20260821T004125Z.log
Result: first start 52s AssertionError fused in_proj_qkvz W4A8 vs I8 shards.
  Patched group_0 += in_proj_qkvz, ignore in_proj_ba. Retry HEALTHY 66s.
  Paris exact, 17*23=391, fib iterative. XPUW4A8IntLinearKernel +
  XPUInt8ScaledMMLinearKernel. Served id qwen3.8-27b-W4A8-rtn-gdn.
Verdict: GO. Serve left Up :18081.
Changed beliefs: vLLM packed_modules_mapping uses in_proj_qkvz not in_proj_qkv$;
  151 regex on HF names is not enough. VLM wrapper required (CausalLM config vs
  language_model. shards). VLLM_W4A8_PREPACKED is an unknown env on 0.26 but the
  mounted patch still honors it.
Next pick: card0 GPTQ fire 2; card1 attach or GRAPH=1 (stop first)
Do not: second serve on card 1; GPTQ on card 1; start DD; bake; P2PACCESS=1
Restore: leases free. Container qwen38_w4a8_rtn holds card 1 GPU. DD stays down.
JOURNAL: ### 2026-08-21e

---

## LOOP 6 -- 2026-08-21T0105Z -- 151 GPTQ fire 2 STARTED

Picked: producer fire 2 (calibrated)
Why this, not the other open row: load-gate GO; campaign 8.1 fire 2
GPU: card0 HELD w4a8-151-gptq pid=353913. card1 serve Up no lease. DD down.
Command: DATAFREE=0 METHOD=gptq SMOOTHQUANT=selective SAMPLES=128 CARD=0 scripts/151
Log: results/logs/151_qwen38_w4a8_20260821_010557.log
Result: STARTED. GDN 144/144. selective-sq 16+64=80 maps. SequentialPipeline.
  OUT will be models/files/qwen3.8-27b/w4a8-gptq-gdn. Hours.
Verdict: STARTED
Changed beliefs: none new (same CausalLM match warning as RTN).
Next pick: ATTACH this job; do not start a second 151
Do not: steal card 0; bake; DD; P2PACCESS=1
Restore: card0 leased until 151 exits. DD stays down.
JOURNAL: ### 2026-08-21f

---

## LOOP 7 -- 2026-08-21T0107Z -- GRAPH=0 attach ~6.3 tok/s

Picked: attach live GRAPH=0 serve (not a new serve)
Why this, not the other open row: dual-card; e2e still had no tok/s
GPU: :18081 qwen38_w4a8_rtn already Up. No new lease.
Command: timed completions warm/128/Paris
Log: n/a (live server)
Result: 6.39 / 6.34 / 6.31 tok/s wall. Paris still exact. Not bench_code c1.
Verdict: GO as GRAPH=0 eager 1-card wall. GRAPH=1 is the decode lever.
Changed beliefs: RTN W4A8 GRAPH=0 is coherent but slow (~6.3). Do not compare to 31.9.
Next pick: ATTACH GPTQ; GRAPH=1 only after stop
Do not: second serve; demote north-star holds
Restore: serve left Up. DD stays down.
JOURNAL: ### 2026-08-21g

---

## LOOP 8 -- 2026-08-21T0135Z -- ATTACH GPTQ layer 9/64 RUNNING

Picked: attach 151 GPTQ (no new GPU job)
Why this, not the other open row: NEXT PICK; card0 HELD by w4a8-151-gptq
GPU: card0 pid=353913 q151_quant. card1 serve Up :18081. DD down.
Command: read results/logs/151_qwen38_w4a8_20260821_010557.log
Log: same
Result: layers 0-9 quantized (62 module steps). Mean 11.8s max 54s.
  ~2.8 min/layer -> ~2.5h left + save. No FAIL. OUT not on disk yet.
Verdict: RUNNING
Changed beliefs: MLP gate/up GPTQ recon error ~5e3 vs GDN out_proj 0.44;
  treat as scale unless Stage B or serve degenerates.
Next pick: ATTACH GPTQ again. wrap_vlm_config.py after Stage B.
Do not: second 151; GRAPH=1 without stopping :18081; DD; bake
Restore: leases unchanged. DD stays down.
JOURNAL: ### 2026-08-21h

---

## LOOP 9 -- 2026-08-21T0205Z -- ATTACH GPTQ layer 30/64 RUNNING

Picked: attach 151 GPTQ
Why this, not the other open row: card0 HELD; NEXT PICK attach
GPU: card0 pid=353913 59m. :18081 Up. DD down.
Command: parse results/logs/151_qwen38_w4a8_20260821_010557.log
Log: same
Result: layers 0-30. 188 module steps. Pace +21 layers / 30m.
  ETA ~50 min + save. No FAIL. OUT not written.
Verdict: RUNNING
Changed beliefs: layer time closer to 1.4 min than 2.8 min once past early layers.
Next pick: ATTACH GPTQ; wrap_vlm_config.py after Stage B
Do not: second 151; GRAPH=1 without stop; DD; bake
Restore: unchanged. DD stays down.
JOURNAL: ### 2026-08-21i
