# Qwen3.8-27B stock 4-bit coding selection

Status: stock Q4_K_M selected and live as the quality-first daily driver on
2026-08-23.

## Measured coding quality

All rows are HumanEval+ 164, thinking off, greedy, generated by the local eval
orchestrator and executed in `evalplus-sandbox:0.3.1`.

| Format and checkpoint | Base pass@1 | Plus pass@1 | Verdict |
|---|---:|---:|---|
| ggml-org stock Q4_K_M | 0.970 | 0.927 | Highest observed; selected |
| AutoRound W4A16 | 0.963 | 0.915 | Close second |
| Inferact ModelOpt NVFP4 | 0.939 | 0.915 | Lower base score |
| RadixArk mixed NVFP4 | 0.933 | 0.890 | Fast NVFP4, lowest coding score here |

Q4_K_M leads W4A16 by one base problem and two plus problems. That is a small
gap and should not be presented as a universal quantization law, but Q4_K_M is
the highest observed result on the common local coding gate. No LiveCodeBench
or matched agentic coding comparison exists for all three formats.

Raw result directories:

- `evals/results/20260817T231333Z___models_Qwen3.8-27B-Q4_K_M.gguf__q4km-0xsero-sycl-tp2`
- `evals/results/20260819T063444Z__qwen3.8-27b-W4A16-autoround-mtp5__W4A16-autoround-mtp5`
- `evals/results/20260816T005900Z__qwen3.8-27b-NVFP4-modelopt-graph-mtp3-pushargraph__nvfp4-modelopt-mtp3`
- `evals/results/20260817T081950Z__qwen3.8-27b-NVFP4-radixark-graph-mtp3-pushargraph__nvfp4-radixark-mtp3`

## Live configuration

- Artifact: `ggml-org/Qwen3.8-27B-GGUF` revision
  `0669b98607d47046c7c2b3f801011d54a08cfccf`.
- File: `Qwen3.8-27B-Q4_K_M.gguf`, 18973870432 bytes.
- SHA256: `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`.
- Topology: TP=2 across both B70 cards, one request lane.
- Context: 262144 with F16 KV.
- MTP off and Q4 lab fusion doors off. This preserves the evaluated coding
  path; the faster doors-on configuration has not rerun HumanEval+.
- Endpoint and model id: `http://192.168.10.5:18080/v1`, `hotschmoe-dd`.
- Shelf: `rdy_to_serve/llamacpp/qwen38-27b-q4km/serve.sh`.

The switch gate passed exact SHA, both-card health, live model identity, native
context identity, Paris coherence, Open WebUI backend visibility, and a fresh
short coding smoke at 35.2 tok/s average. Historical matching code throughput
was 32.8 tok/s, so the current result is faster rather than a regression.

TP=2 is intentional for the first restore because it is the exact topology of
the coding-qualified result. DP=2 would provide two independent request lanes,
but it requires a different KV/context allocation and has not passed the same
stock-model HumanEval+ gate.
