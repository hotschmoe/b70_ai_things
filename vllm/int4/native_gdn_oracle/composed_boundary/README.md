# Confirmed per-prefix native/copy composition

CONFIG -> Adapter image09fc6b750415, TP1 FP8-layout pages, native271db0d4,
B70_XPU_GDN_PREFIX_CONV_COPY=1. Five cases:4796/q4/accepted4 self publication
then running-column move, and4798/q4/accepted1..4 backward-publication controls.
Each runs unsynced and explicitly synced, with identical CPU inputs/initial
state. Exact native source hash, adapter source hashes and selected copy-function
metadata are checked. DESIGN.md retains an earlier held rolling-contract
hypothesis; the executable oracle instead uses the verified per-prefix contract.

COMMAND -> prepare_plan.py freezes oracle.py and the exact standalone CPU
reference source. The selected-card lifecycle owns named/labeled container
cleanup, strict pre/post-health,420second oracle timeout and retained resultJSON.
Default oracle invocation prints a plan; real execution requires --run-xpu and
external gpu-run ownership. No GPU execution occurred during preparation.

RESULT -> CPU tests passed all five independent composed-versus-no-move logical
state contracts. AST guard confirms no .cpu/.item/readback between native1,
actual POST, actual PRE and native2; synchronization there is conditional on
the explicit synced control only. All allocations, uploads and metadata receipts
precede the initial synchronization. PRE bias is computed from actual GPU POST
accepted output; no host accepted-count reconstruction is inserted. Actual
helper methods retain their normal metadata/device-copy work. The first final
readback follows native2. A separately initialized direct native no-move chain
provides an exact logical-state comparator. Mocked lifecycle tests preserve all
ten cases after a numerical failure and run selected post-health after crashes;
failed post-health cannot mask a numerical failure or yield success.

VERDICT -> CPU-reviewed and frozen, GPU qualification pending. Independent
source review found no blocker for this bounded contract. The direct native
operator and copy helper results must agree with independent CPU arithmetic,
valid three-row conv prefixes, unused storage, accepted counts and logical
no-move state. Explicit synced/unsynced raw-state fingerprints must match.
This does not qualify scheduler queues, graph replay, multi-request prefix
reuse or end-to-end model behavior. The initial adapter image has no positive
runtime model-server metadata log; its direct composition receipt verifies
actual selection. Config Env alone is not presented as an observed metadata
receipt. Refusal gates give explicit wrong-native/non-XPU/invalid-flag errors.
