# Native5802 monitored 200K trial

CONFIG -> Exact d556/native6717, TP2/P2P0, MTP3, fresh be02 FP8 KV,
prefix ON, FULL_DECODE_ONLY, 200000 context, stable hotschmoe-dd first.
Explicit user authorization permits monitored serving before long200K
qualification. The complete100K-v2 receipt remains required. The interrupted
200K attempt forcibly killed EngineCore during its 30-second drain budget;
normal_stop=false is preserved. A separate successful rebind, strict per-card
health, and compiled P2P0 collective recovery receipt is required.

COMMAND -> Parent reviews sources, runs prepare_inputs.py then serve.py check.
Only afterward install the repo root override into the existing system service.
The optional complete user unit is an alternative, never a second instance.
No install/start is performed by these preparation scripts. The old trial
record and strict full-qualification service remain unchanged.

RESULT -> Five focused CPU tests pass. Startup requires 24 tiny checks and
64 deterministic/sampled tool checks before the authenticated public frontdoor
opens on 18080; backend remains private on 18124. Existing API key path and
Bearer/X-API-Key semantics are preserved. Separate backend-exit.rc and exit.rc
prevent a clean backend stop from masking startup/frontdoor failures.

VERDICT -> Prepared monitored trial, not completed long200K qualification and
not unrestricted model-quality qualification. Known numeric-array premature
EOS remains recorded. No new inference, GPU touch, install or start here.
