# September 9 cache800K campaign

Status: active maintenance window authorized by the user on 2026-09-09.
Plan: ../../docs/20260909_next_serving_campaign.md.
Raw root: /mnt/vm_8tb/b70/results/cache800k_20260909/.

## Maintenance and CPU preparation

CONFIG -> Live systemd-owned hotschmoe-dd, R276 image521eb277, INT4/MTP3,
FP16 KV, TP2, GPU prefix caching, no RAM tier, 200K/c4/batch32768.
COMMAND -> Preserve live /v1/models, metrics, configuration, system unit,
host packages and lifecycle pointer. Confirm zero running/waiting requests.
Invoke the existing daily launcher's STOP interface; wait for its owned
teardown rather than terminating a GPU worker or bypassing the lease.
RESULT -> Normal server lifecycle exit0, both card probes and compiled
two-rank P2P-off post-health pass. systemd service inactive with Restart=no.
sudo -n systemctl stop required interactive authentication; the already
available hotschmoe-owned lifecycle STOP completed the authorized shutdown.
VERDICT -> Maintenance started cleanly. Original recipe preserved for
restoration. No driver/native-image change. No need for a repeated approval.

CONFIG -> CPU-only inspection of exact R276 and retained SGLang images,
without GPU devices. Existing local source/worktree changes preserved.
COMMAND -> Run campaign CPU tests; export relevant installed source; record
package identity; adapt calibration merge/loading to explicit INT4 identity
and 16 target-only versus 17 target-plus-MTP attention layers.
RESULT -> 14 host CPU tests pass. R276 resolves installed vLLM
0.27.2rc1.dev77+gac7509e2b, Torch2.13.0+xpu, Triton-XPU3.7.2,
oneCCL2022.0.0. New v2 INT4 scales require matching model-config and XPU
routing hashes. Historical official-FP8 schema remains separately identified.
VERDICT -> Calibration intake repaired for the new experiment. No new scales
or FP8 quality result yet. Reference GPU lifecycle underway before calibration.

## SGLang source leads (not GPU results)

CONFIG -> Retained refreshed adc915d266 image; installed source exported
CPU-only under preservation/sglang-source. No archived binary restored.
COMMAND -> Inspect quantization registry, GPTQXPUConfig, xpu_backend and
triton_backend; compare current upstream XPU source read-only.
RESULT -> Dense INT4 has GPTQXPULinearScheme and a native Torch packed-INT4
route. The XPU attention extend path leaves K/V descales unset with an
explicit unsupported-FP8 comment, while decode includes scaling and a query
cast. Triton attention has K/V descale plumbing. These are feature/path
limitations to test, not proof of the cause of the vLLM bangs.
VERDICT -> Qualify the same INT4 weights with FP16 KV first; investigate
Triton attention for calibrated FP8 rather than assume XPU FlashAttention
supports the complete combination. Source-only findings do not qualify
MTP, graphs, hybrid cache reuse or offload on SGLang.

## Fresh reference and offload controls

CONFIG -> Preserved R276 INT4/MTP3/FP16-KV/prefix/graph recipe, fresh process.
COMMAND -> r0-reference.plan.json through cache800k/run_arm.py; short c4
quality followed by three serial 2048-token LRU guides.
RESULT -> Identity matched; 451562 logical KV tokens reproduced; short
quality 24/24 and exact repetition passed. All three guides passed the
coherence detector but were not byte-identical, so the strict arm stopped
before reuse/tool jobs. Isolated CPU functional review passed six code
blocks and 18000 reference operations. Owned teardown and post-health pass.
VERDICT -> Long exact-repeat gate failed even on the current reference.
No bangs observed in these guides; functional correctness is narrower than
determinism. This does not qualify speed or prove an FP8-specific cause.
Continue with explicit MTP0/eager/prefix-off isolation and fresh calibration.

CONFIG -> R276 CPU-only offload tests; candidate image e1fdd5afba6e based
on exact image521eb277, with scheduler backports only.
COMMAND -> test_merged_offload_image.py on base and candidate; candidate
built by build_merged_offload_image.py --base-image with native hash checks.
RESULT -> Base 111 passed/13 failed; candidate 124 passed. All six tracked
native-library hashes unchanged. Candidate scheduler hash
0d2fd9a20e02e2b1560d757658ded738ae6c5d7cc292bf75d20c49636f6338b0.
VERDICT -> CPU scheduler regression gate passed. Actual XPU eviction,
reload correctness, restored hybrid state and reduced recomputation remain
unqualified. This is a candidate image, not a shelf promotion.
