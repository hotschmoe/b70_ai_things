# September 9 cache800K campaign

Status: active maintenance window authorized by the user on 2026-09-09.
Plan: ../../docs/20260909_next_serving_campaign.md.
Raw root: /mnt/vm_8tb/b70/results/cache800k_20260909/.

## Maintenance and CPU preparation

CONFIG -> Live systemd-owned hotschmoe-dd, R276 image521eb277, INT4/MTP3,
FP16 KV, TP2, GPU prefix caching, no RAM tier, 200K/c4/batch32768.
COMMAND -> Preserve live /v1/models, metrics, configuration, system unit,
host packages and lifecycle pointer. Confirm zero running/waiting requests.
Invoke the existing daily launcher's STOP interface; wait for its owned
teardown rather than terminating a GPU worker or bypassing the lease.
RESULT -> Normal server lifecycle exit0, both card probes and compiled
two-rank P2P-off post-health pass. systemd service inactive with Restart=no.
sudo -n systemctl stop required interactive authentication; the already
available hotschmoe-owned lifecycle STOP completed the authorized shutdown.
VERDICT -> Maintenance started cleanly. Original recipe preserved for
restoration. No driver/native-image change. No need for a repeated approval.

CONFIG -> CPU-only inspection of exact R276 and retained SGLang images,
without GPU devices. Existing local source/worktree changes preserved.
COMMAND -> Run campaign CPU tests; export relevant installed source; record
package identity; adapt calibration merge/loading to explicit INT4 identity
and 16 target-only versus 17 target-plus-MTP attention layers.
RESULT -> 14 host CPU tests pass. R276 resolves installed vLLM
0.27.2rc1.dev77+gac7509e2b, Torch2.13.0+xpu, Triton-XPU3.7.2,
oneCCL2022.0.0. New v2 INT4 scales require matching model-config and XPU
routing hashes. Historical official-FP8 schema remains separately identified.
VERDICT -> Calibration intake repaired for the new experiment. No new scales
or FP8 quality result yet. Reference GPU lifecycle underway before calibration.

## SGLang source leads (not GPU results)

CONFIG -> Retained refreshed adc915d266 image; installed source exported
CPU-only under preservation/sglang-source. No archived binary restored.
COMMAND -> Inspect quantization registry, GPTQXPUConfig, xpu_backend and
triton_backend; compare current upstream XPU source read-only.
RESULT -> Dense INT4 has GPTQXPULinearScheme and a native Torch packed-INT4
route. The XPU attention extend path leaves K/V descales unset with an
explicit unsupported-FP8 comment, while decode includes scaling and a query
cast. Triton attention has K/V descale plumbing. These are feature/path
limitations to test, not proof of the cause of the vLLM bangs.
VERDICT -> Qualify the same INT4 weights with FP16 KV first; investigate
Triton attention for calibrated FP8 rather than assume XPU FlashAttention
supports the complete combination. Source-only findings do not qualify
MTP, graphs, hybrid cache reuse or offload on SGLang.

## Fresh reference and offload controls

CONFIG -> Preserved R276 INT4/MTP3/FP16-KV/prefix/graph recipe, fresh process.
COMMAND -> r0-reference.plan.json through cache800k/run_arm.py; short c4
quality followed by three serial 2048-token LRU guides.
RESULT -> Identity matched; 451562 logical KV tokens reproduced; short
quality 24/24 and exact repetition passed. All three guides passed the
coherence detector but were not byte-identical, so the strict arm stopped
before reuse/tool jobs. Isolated CPU functional review passed six code
blocks and 18000 reference operations. Owned teardown and post-health pass.
VERDICT -> Long exact-repeat gate failed even on the current reference.
No bangs observed in these guides; functional correctness is narrower than
determinism. This does not qualify speed or prove an FP8-specific cause.
Continue with explicit MTP0/eager/prefix-off isolation and fresh calibration.

CONFIG -> R276 CPU-only offload tests; candidate image e1fdd5afba6e based
on exact image521eb277, with scheduler backports only.
COMMAND -> test_merged_offload_image.py on base and candidate; candidate
built by build_merged_offload_image.py --base-image with native hash checks.
RESULT -> Base 111 passed/13 failed; candidate 124 passed. All six tracked
native-library hashes unchanged. Candidate scheduler hash
0d2fd9a20e02e2b1560d757658ded738ae6c5d7cc292bf75d20c49636f6338b0.
VERDICT -> CPU scheduler regression gate passed. Actual XPU eviction,
reload correctness, restored hybrid state and reduced recomputation remain
unqualified. This is a candidate image, not a shelf promotion.

## Initial continuation protocol

The MTP0 eager control passed short quality 24/24 exactly. Its first
2048-token guide passed coherence in 330.14 seconds. The original aggregate
900-second bound for three guides is therefore likely too short. This is
a timeout-design issue, not yet an observed third-guide failure.

continue_initial.py waits for owned teardown, then admits exactly one
timeout-only retry with two full guides and an 1800-second job bound.
A correctness failure or unhealthy lifecycle stops the sequence for review.
The retry preserves its own directory and retains the original incomplete
run. Calibration keeps all 262 requests and a larger aggregate bound.

Following healthy complete calibration, the sequence freezes target-only
scales and runs the F1 FP8 held-out audit, two guides, 32K/c2 and 196K/c1
retrieval. It requires 32 loaded rank/layer records, unquantized queries,
short-answer correctness and zero observed held-out clipping before
continuing. These are initial correctness gates, not 800K qualification.
Next is the same-weight SGLang FP16/MTP0/prefix-off/eager control. Each arm
owns its lease, startup identity, teardown and pre/post health.

Plans and continuation-status.json live in the raw root. STOP_CONTINUATION
prevents the next arm; a running arm uses its own STOP lifecycle interface.
There is no automatic shelf promotion, production restart, capacity claim,
or RAM-GPU qualification in this initial sequence. Prefix/MTP/graphs,
800K pressure and real RAM eviction/reload follow review of these gates.

## F0 completion and F1 subprocess import repair

CONFIG -> F0b INT4/MTP0/eager/FP16-KV/prefix-off, installed R276 package.
COMMAND -> Timeout-only retry with two full guides, then 262 calibration
requests including long contexts and continuations; freeze_scales.py.
RESULT -> All jobs and owned lifecycle pass; both guides exactly repeat.
Calibration covers 488317 prompt tokens and all 16 target attention layers
on both ranks. API, engine and worker entry logs show the installed package
and matching XPU routing hash. Frozen scales are in the raw root.
VERDICT -> Target-only calibration collected. Synthetic coverage and static
headroom still need held-out FP8 validation; MTP scales are not qualified.

CONFIG -> F1 FP8 loading with package-identity guard; image default cwd.
COMMAND -> Start F1; inspect failure and reproduce imports in CPU-only
containers, comparing default cwd with --workdir /tmp.
RESULT -> Model-inspection subprocess rejected /workspace/vllm source
checkout before inference. Python -m/-c adds cwd independently of the
already-clean PYTHONPATH. Default cwd imports the checkout; /tmp imports
the installed package. Teardown/recovery and both post-health checks pass.
VERDICT -> Startup/package-selection failure, not an FP8 numerical result.
Set campaign Docker workdir to /tmp, preserving the guard and native bytes.
Fourteen campaign CPU tests pass. Resume in new f1b-fp8-cwd directory, then
SGLang only if initial FP8 gates pass. Original failure remains preserved.

## F1b calibrated FP8 initial gates passed

CONFIG -> INT4/MTP0/eager/prefix-off, calibrated E4M3 KV, FP16 queries,
R276 installed package with /tmp cwd and held-out audit instrumentation.
COMMAND -> f1b-fp8-cwd.plan.json: short audit, two guides, 32K/c2 and
196K/c1 retrieval, owned teardown and health.
RESULT -> All four jobs and lifecycle pass. Short 24/24 and two guides
repeat exactly; retrieval passes at actual 32045/c2 and 196045/c1 prompt
tokens. Sampled audit records 15810560 values each for K and V, zero
clipped values. Allocator reports 1039062 logical KV tokens. Post-health
passes on both cards and compiled two-rank collectives.
VERDICT -> Initial calibrated FP8 correctness gates pass on this exact
MTP0/prefix-off/eager configuration. The pool exceeds 800K, but four
simultaneously progressing 200K contexts have not been demonstrated.
No qualification yet for prefix reuse, MTP, graphs, RAM recovery or speed.

CONFIG -> S0 same-weight SGLang FP16 control.
COMMAND -> s0-sglang-fp16.plan.json.
RESULT -> CLI rejects float16 as --kv-cache-dtype; no model inference.
Owned recovery and post-health pass. This pin accepts auto to inherit
the explicit float16 model dtype.
VERDICT -> Correct launcher to auto and validate the full CLI on CPU;
retry in s0b-sglang-fp16. Original parser failure remains preserved.

## S0b loaded; metrics prerequisite omitted

CONFIG -> Same INT4 tensors, SGLang TP2/MTP0/eager/radix-off, FP16 KV.
COMMAND -> s0b-sglang-fp16.plan.json.
RESULT -> Both ranks load Qwen3_5ForConditionalGeneration with GPTQ bits4;
8.89 GB weight allocation each. Actual KV dtype is torch.float16 and pool
is 633216 logical tokens. Identity endpoint ready. The probe stops before
generation because /metrics returns 404. Owned teardown and post-health pass.
VERDICT -> Harness prerequisite failure, not model correctness failure.
Enable SGLang's documented --enable-metrics flag; retry as s0c. The FP16
pool is below 800K and is not a matched performance comparison to vLLM.
