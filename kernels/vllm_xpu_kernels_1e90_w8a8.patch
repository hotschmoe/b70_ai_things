diff --git a/CMakeLists.txt b/CMakeLists.txt
index 6fb8939..f7f9d2a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -619,6 +619,7 @@ if(XPU_SPECIFIC_KERNELS_ENABLED)
 
   set(VLLM_EXT_XPU_SRC
       "csrc/xpu/torch_bindings.cpp"
+      "csrc/xpu/quantization/int8_quant.cpp"
       "csrc/xpu/lora/lora_shrink.cpp"
       "csrc/xpu/lora/lora_expand.cpp"
       "csrc/xpu/lora/lora_ops.cpp"
diff --git a/csrc/xpu/onednn/onednn_ext.h b/csrc/xpu/onednn/onednn_ext.h
index 69f64f9..a498251 100644
--- a/csrc/xpu/onednn/onednn_ext.h
+++ b/csrc/xpu/onednn/onednn_ext.h
@@ -43,6 +43,8 @@ enum class joint_dtypes_t {
   bf16_int4,
   s8_int4,
   u8_int4,
+  s8_s8_f16,
+  s8_s8_bf16,
   f16_f8_e5m2,
   bf16_f8_e5m2,
   f16_f8_e4m3,
@@ -103,6 +105,26 @@ struct onednn_types_mapper<joint_dtypes_t::u8_int4> {
   }
 };
 
+template <>
+struct onednn_types_mapper<joint_dtypes_t::s8_s8_f16> {
+  static inline std::
+      tuple<memory::data_type, memory::data_type, memory::data_type>
+      get() {
+    return std::make_tuple(
+        memory::data_type::s8, memory::data_type::s8, memory::data_type::f16);
+  }
+};
+
+template <>
+struct onednn_types_mapper<joint_dtypes_t::s8_s8_bf16> {
+  static inline std::
+      tuple<memory::data_type, memory::data_type, memory::data_type>
+      get() {
+    return std::make_tuple(
+        memory::data_type::s8, memory::data_type::s8, memory::data_type::bf16);
+  }
+};
+
 template <>
 struct onednn_types_mapper<joint_dtypes_t::f16_f8_e5m2> {
   static inline std::
@@ -1040,6 +1062,36 @@ matmul_primitive_create_and_cache(
           scale_group_size,
           zp_group_size,
           batch_dim);
+    case joint_dtypes_t::s8_s8_f16:
+      return matmul_primitive_create_and_cache<joint_dtypes_t::s8_s8_f16, F>(
+          Tt,
+          b_type,
+          m,
+          n,
+          k,
+          lda,
+          ldb,
+          ldc,
+          device_id,
+          attr,
+          scale_group_size,
+          zp_group_size,
+          batch_dim);
+    case joint_dtypes_t::s8_s8_bf16:
+      return matmul_primitive_create_and_cache<joint_dtypes_t::s8_s8_bf16, F>(
+          Tt,
+          b_type,
+          m,
+          n,
+          k,
+          lda,
+          ldb,
+          ldc,
+          device_id,
+          attr,
+          scale_group_size,
+          zp_group_size,
+          batch_dim);
     case joint_dtypes_t::f16_f8_e5m2:
       return matmul_primitive_create_and_cache<joint_dtypes_t::f16_f8_e5m2, F>(
           Tt,
diff --git a/csrc/xpu/onednn/onednn_matmul.cpp b/csrc/xpu/onednn/onednn_matmul.cpp
index a971508..096d797 100644
--- a/csrc/xpu/onednn/onednn_matmul.cpp
+++ b/csrc/xpu/onednn/onednn_matmul.cpp
@@ -4,6 +4,7 @@
 #include "fp8_gemm_w8a16.h"
 #include "int4_gemm_w4a16.h"
 #include "int4_gemm_w4a8.h"
+#include "int8_gemm_w8a8.h"
 
 inline bool is_supported_fp8(at::ScalarType t) {
   return (t == at::ScalarType::Float8_e5m2) ||
@@ -326,3 +327,22 @@ torch::Tensor int4_gemm_w4a8(
 
   return result;
 }
+
+torch::Tensor int8_gemm_w8a8(
+    const torch::Tensor& A,
+    const torch::Tensor& A_scale,
+    const torch::Tensor& B,
+    const torch::Tensor& B_scale,
+    std::optional<c10::ScalarType> out_dtype,
+    const std::optional<torch::Tensor>& bias) {
+  const at::DeviceGuard device_guard(A.device());
+  TORCH_CHECK(A_scale.is_contiguous(), "A_scale must be contiguous");
+  TORCH_CHECK(B_scale.is_contiguous(), "B_scale must be contiguous");
+  TORCH_CHECK(!bias.has_value() || bias.value().is_contiguous(),
+              "bias must be contiguous");
+  torch::Tensor result = check_and_create_output_tensor(A, B, out_dtype);
+  const bool is_nt = B.strides()[B.dim() - 2] == 1;
+  oneDNN::dnnl_matmul_w8a8_int8(
+      result, A, B, is_nt, bias, A_scale, B_scale);
+  return result;
+}
diff --git a/csrc/xpu/ops.h b/csrc/xpu/ops.h
index 9f6502e..aa3e93d 100644
--- a/csrc/xpu/ops.h
+++ b/csrc/xpu/ops.h
@@ -75,6 +75,22 @@ torch::Tensor int4_gemm_w4a8(
     const std::optional<torch::Tensor>& g_idx,
     const std::optional<torch::Tensor>& bias);
 
+torch::Tensor int8_gemm_w8a8(
+    const torch::Tensor& A,
+    const torch::Tensor& A_scale,
+    const torch::Tensor& B,
+    const torch::Tensor& B_scale,
+    std::optional<c10::ScalarType> out_dtype,
+    const std::optional<torch::Tensor>& bias);
+
+std::tuple<torch::Tensor, torch::Tensor> per_token_quant_int8_xpu(
+    const torch::Tensor& x);
+
+std::tuple<torch::Tensor, torch::Tensor> per_token_quant_int8_xpu_out(
+    const torch::Tensor& x,
+    torch::Tensor& q,
+    torch::Tensor& scales);
+
 #ifdef VLLM_MOE_ENABLED
 torch::Tensor cutlass_grouped_gemm_interface(
     torch::Tensor ptr_A,
diff --git a/csrc/xpu/torch_bindings.cpp b/csrc/xpu/torch_bindings.cpp
index 8f44952..d86866b 100644
--- a/csrc/xpu/torch_bindings.cpp
+++ b/csrc/xpu/torch_bindings.cpp
@@ -62,6 +62,24 @@ TORCH_LIBRARY_EXPAND(TORCH_EXTENSION_NAME, xpu_ops) {
       "bias) -> Tensor");
   xpu_ops.impl("int4_gemm_w4a8", torch::kXPU, &int4_gemm_w4a8);
 
+  xpu_ops.def(
+      "int8_gemm_w8a8(Tensor A, Tensor A_scale, Tensor B, Tensor B_scale, "
+      "ScalarType? out_dtype, Tensor? bias) -> Tensor");
+  xpu_ops.impl("int8_gemm_w8a8", torch::kXPU, &int8_gemm_w8a8);
+
+  xpu_ops.def(
+      "per_token_quant_int8_xpu(Tensor x) -> (Tensor, Tensor)");
+  xpu_ops.impl(
+      "per_token_quant_int8_xpu", torch::kXPU, &per_token_quant_int8_xpu);
+
+  xpu_ops.def(
+      "per_token_quant_int8_xpu_out(Tensor x, Tensor(a!) q, Tensor(b!) "
+      "scales) -> (Tensor(a!), Tensor(b!))");
+  xpu_ops.impl(
+      "per_token_quant_int8_xpu_out",
+      torch::kXPU,
+      &per_token_quant_int8_xpu_out);
+
 #ifdef VLLM_MOE_ENABLED
   xpu_ops.def(
       "cutlass_grouped_gemm_interface(Tensor ptr_A, Tensor? ptr_A_scale, "
