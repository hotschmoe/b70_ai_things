# Shared Historical Scripts

Only backend-agnostic or currently relevant scripts remain here. New serving and
experiment scripts belong under `sglang/` or `vllm/`.

- `150_quantize_qwen38_27b_w8a8.sh`: retained Qwen3.8 W8A8 build recipe.
- `allreduce_bench.py`: compiled two-rank collective health helper.
- `bw_p2p_probe.py`: B70 peer bandwidth and topology probe.

The older append-only script notebook is in the gitignored 2026-08-26 cleanup
quarantine.
