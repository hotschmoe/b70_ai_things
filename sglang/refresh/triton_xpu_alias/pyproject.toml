[build-system]
requires = ["setuptools==80.9.0", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"

[project]
name = "triton"
version = "3.7.2"
description = "Metadata alias for the Torch XPU triton-xpu distribution"
requires-python = ">=3.9"
dependencies = ["triton-xpu==3.7.2"]

[tool.setuptools]
packages = []
