apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: false
    # The shipped sglang-dashboard.json hard-codes this datasource uid in its panels; pin it here so the
    # provisioned datasource matches (otherwise panels show "datasource not found"). Do not change without
    # also updating the dashboard JSON.
    uid: ddyfngn31dg5cf
